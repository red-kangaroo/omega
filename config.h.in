// Omega is free software, distributed under the MIT license
// config.h generated by configure
#pragma once

// Define to the one symbol short name of this package.
#define @pkg_uname@_NAME	"@pkg_name@"
// Define to the version of this package.
#define @pkg_uname@_VERSION	@pkg_version@
// Define to the version string of this package.
#define @pkg_uname@_VERSTRING	"@pkg_verstr@"
// Define to the address where bug reports for this package should be sent.
#define @pkg_uname@_BUGREPORT	"@pkg_bugreport@"

// Location of the game data files
#define OMEGALIB		"./lib/"
#define OMEGA_CONFDIR		"%s/.config/omega/"
#define OMEGA_PLAYER_FILE	OMEGA_CONFDIR "player.dat"
#define OMEGA_SAVED_GAME	OMEGA_CONFDIR "game.sav"

// Saved data versions
#define OMEGA_PLAYER_FORMAT	0
#define OMEGA_SAVE_FORMAT	0

// Define to 1 to use ucc
#undef USE_UCC
#if USE_UCC
    // Define to 1 if have ucc
    #define HAVE_STL_H 1
    #if HAVE_STL_H
	#include <stl.h>
    #endif
    #if UC_VERSION < 0x11
	#error "ucc version 1.1 or later required to compile this package"
    #endif
    using bstri = istream;
    using bstro = ostream;
    using bstrs = sizestream;
    template <typename Ctr> inline typename Ctr::iterator p2i (Ctr&, typename Ctr::pointer p) { return p; }
    static inline unsigned xrand (void)		{ return rand(); }
    static inline void sxrand (unsigned seed)	{ srand (seed); }
#else
    #include "uccshim.h"
#endif
